import type { useUserProfileModel } from './user-profile.model';

type UserProfileViewProps = ReturnType<typeof useUserProfileModel>;

export function UserProfileView({}: UserProfileViewProps) {
  return <></>;
}
